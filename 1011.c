#include <stdio.h>
 
int main() {
        double raio;
        double pi = 3.14159;

        scanf("%lf",&raio);

        printf("VOLUME = %.3f\n",(4.0/3.0) * pi * (raio*raio*raio));

    return 0;
}
